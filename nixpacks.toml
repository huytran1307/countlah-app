[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["npm install -g pnpm", "pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["pnpm --filter @countlah/api run build"]

[start]
cmd = "node --enable-source-maps apps/api/dist/index.mjs"
