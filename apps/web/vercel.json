{
  "buildCommand": "cd ../.. && pnpm --filter api run build && pnpm --filter web run build",
  "outputDirectory": "dist/public",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "RAILWAY_URL_PLACEHOLDER/api/:path*"
    }
  ]
}
